@model IEnumerable<PL.Models.AnswerViewModel>


    <h2>Index</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.content)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.content)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ISCorrect)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.content)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.content)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ISCorrect)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Answer_ID }) |
                @Html.ActionLink("Details", "Details", new { id=item.Answer_ID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Answer_ID })
            </td>
        </tr>
        }

    </table>
